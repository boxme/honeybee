# Database Configuration
# For production, DigitalOcean will provide this automatically
# SSL is automatically enabled in production (when NODE_ENV=production)
DATABASE_URL=postgresql://username:password@localhost:5432/honeybee

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000

# Server Port
PORT=3001

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Node Environment (development or production)
# When set to 'production', SSL is enabled for database connections
NODE_ENV=development
